[project]
name = "custom_design"
authors = [
    { name = "Sibyl Technologies", email = "hello@tijaratapp.com" }
]
description = "No-code theme, branding, and navigation customization for ERPNext."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
